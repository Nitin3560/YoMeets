{
  "name": "@yomeets/benchmark-phase1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --pretty false",
    "test": "tsc -b --pretty false && node dist/index.test.js && node dist/meeting-benchmark.test.js",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@yomeets/agent-core": "workspace:*",
    "@yomeets/browser-core": "workspace:*",
    "@yomeets/model-router": "workspace:*",
    "@yomeets/storage": "workspace:*",
    "@yomeets/task-engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
