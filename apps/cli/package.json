{
  "name": "@yomeets/cli",
  "private": true,
  "type": "module",
  "bin": {
    "yomeets": "dist/main.js"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b && node dist/main.js serve",
    "lint": "tsc -b --pretty false",
    "test": "tsc -b --pretty false",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@yomeets/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
