{
  "name": "@yomeets/chrome-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && node scripts/prepare-observer.cjs && cp manifest.json dist/manifest.json",
    "dev": "tsc -b && node scripts/prepare-observer.cjs && cp manifest.json dist/manifest.json && tsc -b --watch --preserveWatchOutput",
    "lint": "tsc -b --pretty false",
    "test": "tsc -b --pretty false",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@yomeets/protocol": "workspace:*",
    "@yomeets/shared": "workspace:*"
  }
}
