{
  "name": "@yomeets/task-engine",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "lint": "tsc -b --pretty false",
    "test": "tsc -b --pretty false && node dist/intent.test.js && node dist/parser.test.js",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@yomeets/model-router": "workspace:*",
    "@yomeets/storage": "workspace:*",
    "zod": "^4.4.3"
  }
}
